import { test, expect } from '@playwright/test';

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
    // generate code here.
  //Open amazon cart, click 'Go to Cart' button, then open login page and fill email field.
  });
});
